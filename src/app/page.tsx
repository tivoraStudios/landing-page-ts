import { HomePageContainer } from "@/containers/home-page";
import React from "react";

const HomePage = () => {
    return (
        <div>
            <HomePageContainer />
        </div>
    );
};

export default HomePage;
